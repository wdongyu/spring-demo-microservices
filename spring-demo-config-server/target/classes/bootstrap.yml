server.port: 8887
spring:
  cloud:
    config:
      server:
        git:
          uri: file:///home/wdongyu/spring-demo-microservices-config
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
