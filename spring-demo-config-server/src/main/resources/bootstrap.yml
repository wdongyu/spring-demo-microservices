server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: file:///home/wdongyu/Documents/spring-demo-microservices-config
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
